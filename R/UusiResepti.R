
# Define UI for application that draws a histogram
UusiReseptiUI <- function(id){ 
  ns <- NS(id)
  fluidPage(
    h5("Lisää uusi resepti")
  )
}
# Define server logic required to draw a histogram
UusiReseptiServer <- function(input, output, session) {
  
  
}